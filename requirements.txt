numpy==2.1.3
faiss-cpu==1.9.0.post1
# M5b-3: transformers 5.x is required by Qwen3-ASR (the core ASR engine);
# sentence-transformers upgraded in lockstep (3.x pins transformers<5).
sentence-transformers==6.0.1
transformers==5.16.1
librosa==1.0.0
# torch 2.14 + torchaudio 2.11: torchaudio stays on 2.11 (works, kaldi
# compliance path only — used by IndicConformer's remote code).
torch==2.14.0
torchaudio==2.11.0
llama-cpp-python==0.3.35
faster-whisper==1.1.1
piper-tts==1.7.0
pytest==8.3.4
# 2026-09-04: LiveKit telephony limb — realtime client SDK (provides
# `livekit.rtc`; the monorepo source dir is `livekit-rtc/` but no
# `livekit-rtc` distribution exists on PyPI) + server API SDK.
livekit==1.1.17
livekit-api==1.2.1
